apiVersion: getambassador.io/v2
kind: FilterPolicy
metadata:
  name: "facesystems-filter-policy"
spec:
  rules:
  - host: "*"
    path: "/otusapp/vaagn/api/users/*"
    filters:
    - name: "facesystems-jwt-filter"

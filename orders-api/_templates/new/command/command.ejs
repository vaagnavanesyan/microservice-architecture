---
to: src/orders/commands/impl/<%= name %>.command.ts
---
import { <%= h.changeCase.pascal(name)%>Payload } from '../payloads/<%= name %>.payload';

export class <%= h.changeCase.pascal(name) %>Command {
  constructor(public readonly payload: <%= h.changeCase.pascal(name)%>Payload) {}
}

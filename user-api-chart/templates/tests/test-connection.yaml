apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "user-api-chart.fullname" . }}-test-connection"
  labels:
    {{- include "user-api-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "user-api-chart.fullname" . }}:{{ .Values.servicePort }}']
  restartPolicy: Never

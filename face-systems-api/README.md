user-api - user registration and authorization service. In future - API GateWay, BFF

## Metrics

- `user_api_requests` - requests count by `HTTP` methods

- `user_api_latency` - response times with quantiles: 0.5, 0.95, 0.99, max
